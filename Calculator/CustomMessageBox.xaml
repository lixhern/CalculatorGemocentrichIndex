<Window x:Class="Calculator.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator"
        mc:Ignorable="d"
        Title="Результат" Height="184" Width="283">
    <Grid>
        <TextBlock x:Name="MessageText" VerticalAlignment="Center" HorizontalAlignment="Center" 
               TextWrapping="Wrap" Margin="20,20,20,40"/>

        <!-- Контейнер для кнопок с вертикальной ориентацией -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                Margin="0,0,0,20">
            <!-- Кнопка Закрыть -->
            <Button Content="Закрыть" Width="120" Height="30" Margin="0,0,10,0" Click="CloseWindw_Click"/>

            <!-- Кнопка Печать -->
            <Button Content="Печать" Width="120" Height="30" Margin="10,0,0,0" Click="PrintInfo_Click"/>
        </StackPanel>

    </Grid>
</Window>
